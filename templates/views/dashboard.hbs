<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <style>

    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      color: #ffffff; /* Set text color to white or any color that suits your background */
      overflow: hidden; /* Hide overflow to prevent blurred image from causing scrollbars */
      position: relative; /* Establish stacking context for pseudo-element */
    }

    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('img/unsplash_cricket.svg'); /* Replace with the path to your image */
      background-size: cover; /* This will make the image cover the entire pseudo-element */
      background-position: center; /* Center the background image */
      background-repeat: no-repeat; /* Do not repeat the background image */
      filter: blur(6px); /* Apply a blur effect to the background image */
      z-index: -1; /* Position the pseudo-element behind the content */
    }

    
    #welcome-message {
      font-size: 15px;
      font-weight: 400;
      color: #ffffff;
      position: absolute;
      top: 100px;
      left: 10px;

    }
    .rectangle {
      width: 1400px;
      height: 50px;
      background-color: #000000;
      color: #ffffff;
      text-align: left;
      padding-left: 10px;
      line-height: 50px; /* Center the text vertically */
      font-size: 20px;
      font-weight: 400;
      color: #ffffff;
      position: absolute;
      top: 90px;
      left: -2px;
      box-shadow: inset 0px -5px 10px -10px #fefeff;
}
  </style>
  </head>

<body>
  <!-- Navbar (sit on top) -->
  {{>admin_navbar}}

    <div>
        <div class="rectangle">
            Welcome, {{userName}}!
              </div>
        
 
 

  <footer>
    {{>footer}}
  </footer>
</body>

</html>
